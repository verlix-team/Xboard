# Xboard 仓库专项规则

本文件适用于 Xboard 仓库。若处于完整 Hop 工作区，先遵守上级 `../AGENTS.md`；单独克隆本仓库时，本文件仍是最低执行基线。

## 1. 项目角色与开始前检查

- Xboard 是 Java 用户后台的业务与结构参考，同时继续运行管理后台及已确认保留的调度、Telegram、佣金、工单、节点协议和运维能力。
- 修改前检查 `git status --short`、分支、HEAD 和现有暂存区；保护用户已有改动。
- 分析用户功能必须追踪 Route、Controller、Request、Resource、Service、Model、Repository、Job/Event/Listener/Hook、Schedule、当前主题调用和全部相关 Laravel Migration，不能只搜索同名类。
- `功能清单.md` 是基于源码和当前主题入口的功能概览；`Xboard源码与业务分析.md` 是调用链基线，二者都不替代 Java 本地首发状态和验收证据。
- Java 当前首发状态先看 `../hop/docs/Java用户后台当前状态摘要.md`，已确认差异和处理权见 `../hop/docs/Java用户后台实现计划与进度.md`；模块历史明细按需看 `../hop/docs/Java用户后台模块详细实施归档.md`，Xboard 业务基线见 `Xboard源码与业务分析.md`。

## 2. 修改授权与运行边界

- 默认把 Xboard 当作参考和仍在运行的管理/外部控制面；只有用户明确要求修复、加固、兼容或实现已确认处理权时才修改运行源码。
- 不为 Java 用户后台复制或删除 Xboard 管理端路由。源码中失效或无调用的遗留入口先分类，不机械迁移。
- Telegram 外部调用、支付渠道、真实节点协议、定时任务处理权和生产开关在未确认时只分析；实施后默认关闭，并记录部署、启用、回滚和对账。
- Xboard 原生表结构由本仓库 Laravel Migration 管理。Java `java_` 扩展表仍由 Java Flyway 管理；Xboard 仅按已确认运行职责读写其业务数据，不在 Laravel Migration 重建 Java 表。
- Xboard 与 Java 写同一原表时按业务操作和字段划分权限。不得用 Laravel `onOneServer()` 或本地缓存代替跨运行时数据库处理权。

## 3. 共享数据与副作用

- 修改共享表写入前检查 Model Observer、Event、Hook、缓存失效、队列和定时任务完整副作用，并确认 Java 直写时是否需要持久补偿。
- 金额统一明确分/元/渠道最小单位；流量明确字节/GB；Unix 秒与 `DATETIME` 不混用。
- 状态推进、佣金、工单、订单、流量和节点增量必须幂等；跨实例依赖条件更新、唯一约束、行锁、claim token 或持久账本。
- 外部网络调用不长期占用数据库事务；未知结果不盲目重试。日志不输出 Token、密码、验证码、支付/SMTP/Telegram 密钥或完整敏感载荷。
- Hop traffic Outbox 短期方案中，Xboard 是唯一节点控制面消费者；开关默认关闭。不得让 Java 与 Xboard 同时消费，不得删除未完成 Outbox/交付账本，离线节点依赖权威全量同步收敛。

## 4. 测试与文档

- PHP 修改至少执行受影响文件 `php -l` 和目标 PHPUnit；有 PHPStan 时检查新增/修改路径，并区分本次错误与既有动态属性告警。
- 不在连接共享真实数据库的配置下盲目运行会刷新、回滚、截断或清理数据的完整 Laravel 测试套件。先检查测试基类、环境和数据库指向。
- `--dry-run` 必须保持只读，不抢占任务、不发布 Redis/WS、不弹出旧队列/集合、不写状态。
- 修改 Xboard 运行源码后，同步更新 `../hop/docs/Java用户后台实现计划与进度.md` 的“Xboard 源码修改记录”；架构或处理权变化时同步 Java 架构文档，日期证据写验收归档。
- 仅整理 Xboard 说明文档时，保持源码基线、功能概览和历史部署快照的边界；Java 首发状态、后续排期和日期证据分别回链 Java 进度、后续清单和验收归档。
- 交付前运行 `git diff --check`，报告实际运行和未运行的测试、外部环境、共享数据影响、开关状态与回滚条件。
- 未经用户明确要求不提交、不推送、不部署、不启用调度或外部服务。

## 5. 按需详细规范

新功能、金额/状态/权限冲突、共享数据库治理或双运行时切换时，读取 `../hop/docs/Agent协作与执行详细规范.md` 的相关章节；普通源码定位、验收和小修复不整篇加载。
