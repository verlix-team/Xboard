# 原管理后台套餐国际化扩展

## 范围

修复目标是Xboard原管理入口，不是独立 `hop-admin-webui`。开发 `[开发完成]`、验收 `[部分通过]`；2026-09-17授权发布结果在下方，2026-09-18重复编辑区补修仅本地、尚未提交发布。沿用已发布的套餐 fetch/save 翻译合同，不新增业务写接口、不改价格/权益/订单快照或数据库结构。

`public/assets/admin` 为固定产物子模块，只有编译文件，没有React组件源码。本批初始化已固定 `ef5f43da335092cbff8fdf0ad7ff9b4d92d7d0d7`，子模块工作树干净、gitlink不变；没有改写/推送上游子模块。

## 维护方式

- `OriginalAdminAssetService`严格校验原入口文件SHA256，再检查四处唯一集成锚点：原handleSubmit合并翻译；原form顶部渲染国际化编辑器并移除旧名称控件；移除旧说明整个控件；原内部名称校验上限对齐后端255。不是CSS隐藏或运行时DOM删除，升级上游须重新审查所有锚点。
- `public/assets/hop-admin/plan-translations.js`是可维护扩展源码，复用原React与form实例。没有DOM推断、XHR/fetch拦截、Token读取、iframe或第二后台跳转。
- 原页面通过Blade加载带内容版本的新JS资产URL，HTTP路由组合扩展与原包；仍在原assets/admin/assets目录，保持Monaco Worker相对资源位置。组合不写磁盘或修改原产物。资源处理函数只读静态文件，不读取设置、账号或数据库；框架既有全局插件/运行时设置中间件仍保留，不宣称整个HTTP请求无需数据库。响应JS MIME/nosniff及内容版本永久缓存；版本改变即换URL。
- 固定包指纹或锚点不匹配时拒绝生成，不盲目给新上游包打补丁。升级固定子模块必须重新审查入口、React/form变量及唯一锚点，运行PHP/浏览器回归；不能仅更新gitlink便宣称国际化仍可用。恢复旧Blade/路由/服务版本即可撤去扩展，保留共享翻译表与历史快照。
- 使用原弹窗“提交”一次保存套餐与全部翻译，同事务/行锁/409保护保持在现有Controller；价格、流量、force_update参数不重新计算或改写。未迁移、缺版本或套餐上下文不匹配时前端阻止提交，不偷偷保留/猜测翻译。
- 中英俄独立名称/说明、唯一默认回退语言、显式原文导入、草稿保留、空语言删除。说明使用文本源编辑，支持既有Markdown/HTML/feature-support JSON；服务端检查各语言特性数量、顺序及支持标记。不是富文本渲染预览，不自动翻译。
- 原套餐弹窗只显示国际化名称/说明，不再保留下方旧名称/说明编辑区。已有套餐内部name/content保持原值；新建时从用户明确选择的默认翻译填充内部必填name/content，以通过原表单/后端校验，不额外展示第二套字段。切换正在编辑的语言不改变默认项；默认项改动才改变新建内部取值。原文导入仅已有套餐可见。

## 本地验证（2026-09-17）

PHP命令（工作区根）：

```sh
php Xboard/vendor/bin/phpunit --no-configuration --bootstrap Xboard/vendor/autoload.php Xboard/tests/Unit/Services/OriginalAdminAssetServiceTest.php Xboard/tests/Unit/Services/PlanTranslationServiceTest.php Xboard/tests/Unit/Services/PlanTranslationTransactionTest.php
```

14项/67断言通过（前批66，发布前新增CSS内容指纹URL断言）。新增测试只读固定包，不启动应用或连接数据库；原服务测试独立验证器/SQLite内存库。服务/路由/测试PHP语法与组合包ES模块语法检查通过。发布前CSS改为原assets/admin目录中的指纹文件名路由，不带查询参数，使既有部署HTML解析器也完整检查新增CSS的200/MIME，无需修改或放宽部署门禁。

Node目标逻辑测试：`node --test Xboard/tests/JavaScript/plan-translations.test.mjs`，5项通过，覆盖原价格/权益参数不变、完整翻译/版本、错套餐/缺版本/缺元数据/关闭草稿阻止提交、不猜测语言、Unicode名称与UTF8描述限额。

浏览器：`node Xboard/tests/Browser/OriginalAdminPlanLocalization.mjs`，需要Playwright Chromium，默认复用工作区用户前端已安装的Playwright，可用 `PLAYWRIGHT_MODULE`指定模块。启动唯一临时回环端口，加载实际固定原包及扩展，虚拟账号与本机Mock API，不访问真实管理/用户API；结束自动关闭浏览器和服务器。原Hash路由、虚拟套餐91原弹窗，中英俄草稿保留、默认不被语言切换覆盖、原提交携带三语言/原版本、重新打开、原17.99价格/100GB参数不变通过。桌面1440×1100及移动390×844截图人工查看通过，0页面错误/0横向溢出。初次隔离预览路由和UTF8编码错误已修正，不能写成服务端业务失败。

截图：[桌面](assets/2026-09-17-original-plan-i18n/editor-1440.png)、[移动](assets/2026-09-17-original-plan-i18n/editor-390.png)。

本地开发没有新增数据库写入或真实业务测试数据，没有读取共享 `.env`；真实原入口资源、管理员登录保存/重开/409与两台Web滚动部署实际结果另行记录。按当前提交/推送/部署授权，仅发布两台测试Web；不重建WS/Horizon或Java，不迁移数据库/改业务开关。不能把独立管理前端部署当成原入口修复已上线。详细证据见Java验收第94–95节。

## 双机测试发布（2026-09-17）

用户明确授权后，Xboard/test功能提交 `e142e17d039f66972926139c79e9d16fb8a6d01b`已推送；正式Web滚动脚本退出0。两台原Web均运行 `xboard-local:e142e17d039f`，镜像 `sha256:1c6ac93d77dfd7aa6ba15a61439c9a5da34dab0dbea9c09cb2c28a835bfc900d`；第一台10秒、第二台15秒就绪，restart0。原管理HTML及新指纹JS/CSS/语言资源通过本地、逐台指定IP与公开 `panel.verlix.biz` HTTPS的200/MIME门禁。组合JS实际正文SHA256 `dce92c6c3be6b8c96a5962665ff216c38248e8f7fb6327aa57939730cbf79b02`、CSS `baec2deebaa895fc1a252b2b32e2506d2ffce01c1b5e8b40299c074b371734aa`与部署源码一致，nosniff/immutable成立，错误版本返回404。

备份批次 `20260917T131438Z`保留两台Compose/受保护运行配置及共享DB备份，旧Web镜像 `xboard-local:2a8def1334bc`可按正式脚本流程回退。无新增Laravel Migration、DDL或业务数据写入；不重复V026/中文初始化，插件开关及WS/Horizon快照不变，独立管理/Java/用户Web没有再次部署。源码及发布记录后续文档提交与实际功能镜像e142e17明确区分。

原后台原套餐弹窗顶部已接入中英俄名称/说明和默认语言编辑区；刷新原入口后打开编辑即可查看。真实管理员登录HTTP保存/重开/409仍待验收；固定原包隔离浏览器测试和公开资源校验不能替代此项。总体验收仍 `[部分通过]`，英俄需要管理员填写，不自动生成翻译。

## 重复编辑区补修（2026-09-18，本地未发布）

用户截图反馈顶部国际化与下方旧名称/说明并存。旧表单控件没有被移除，是前次补修遗漏，不是缓存或翻译缺失。现按精确源码指纹/唯一锚点移除旧名称与完整旧说明控件，标签恢复单列；原价格、分组、权益字段与提交按钮不动。新建默认语言内部取值及255字名称可正常保存，已有内部字段不因翻译编辑而改写。

修改服务PHPStan现有level5单进程检查0错误；PHP/组合包ES模块语法、文档一致性及差异空白检查通过。

PHP14项71断言、Node6项通过；实际固定原包隔离浏览器桌面1440×1100及手机390×844断言无旧标签、name/content输入框或旧富文本控件，仅一个国际化区域。原套餐三语言保存/重开、价格17.99及100GB/内部原文保持通过；新建默认英语、当前编辑俄语、255字默认名称提交通过，价格9.99/100GB保持，0页面错误/溢出。截图：[桌面](assets/2026-09-18-single-plan-editor/editor-1440.png)、[手机](assets/2026-09-18-single-plan-editor/editor-390.png)。仅本地Mock，不写共享库；尚未提交/推送/部署或做管理员登录验收，既有测试部署e142e17不等于本次代码已上线。

## 集中复测与发布授权（2026-09-18）

用户最新明确要求自行复测、没问题后提交/推送/部署。PHP14项71断言、Node6项、PHPStan修改服务level5/0错误与语法检查重跑通过；原固定包中英俄界面×桌面/手机6组交互通过，覆盖原保存/重开、内部字段/价格/权益保持、新建255字默认名称、空默认名称拒绝且0保存请求、取消草稿不带入新建、删除非默认翻译。初次新增删除测试在手机布局使用桌面row定位超时，改为明确切回桌面后3种语言重跑全通过；不是业务服务失败，未忽略断言。

浏览器脚本可设置 `ORIGINAL_ADMIN_PREVIEW_LOCALE=zh-CN|en-US|ru-RU`；`ORIGINAL_ADMIN_SCRIPT_FILE`可加载已核对指纹的公开部署JS，用同一隔离Mock回归实际发布字节，不读取真实账号或Token。不同界面语言截图分开输出。依旧不是管理员JWT/HTTP中间件或真实数据库保存证明。实际双机发布结果另行记录，范围只Web，不改变数据库/插件/WS/Horizon/Java或独立管理。
