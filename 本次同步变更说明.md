# Java 用户后台配套改造同步说明

## 同步范围与版本

- 日期：2026-09-04。
- 来源：工作区 `Xboard`，源提交 `b004bfe`。
- 共同基线：`60ba571`。
- 目标：`Xboard-test`，同步前提交 `4f48e61`，分支 `master`。
- 同步来源相对共同基线的 30 个文件改动；另新增本说明文件。
- 本次只修改工作区文件，没有暂存、提交、推送、部署、执行数据库迁移或启用功能开关。
- 保留目标仓库自带的 5 个后续上游提交，不将目标仓库整体覆盖为旧版本。

## 业务变更

1. 订单处理权隔离与结构兼容：增加 `v2_order.processor`、`active_user_id`、活动订单唯一约束和查询索引；订单扫描与队列执行均排除 `JAVA_USER`。旧订单处理方默认为 `LEGACY`。
2. 支付配置 UUID 唯一性：新增正式 Laravel Migration，检查重复支付 UUID 后建立唯一索引，不自动改写或删除支付配置。
3. 停用支付插件管理编辑：允许管理端读取已安装但停用插件的既有配置表单；用户支付仍拒绝停用插件，不额外启动插件路由或任务。
4. 空站点配置兼容：管理端保存 PHP NULL 时删除对应设置项并清理缓存，统一为“未配置”语义。
5. Java 流量变化通知节点：增加 Outbox 消费、逐节点交付、认领租约、重试退避、当前状态重算和传输层确认；复用 Xboard 节点控制面，开关默认关闭，传输层确认不等于节点应用层确认。
6. 流量重置并发保护：在事务内重读并锁定用户行，自动重置拿锁后重新判断是否到期，防止基于旧数据重复重置。
7. 管理封禁联动 Java 凭证撤销：单用户和批量封禁在同一事务内写入 Java 撤销请求，写入失败则封禁回滚；Java 独占请求消费和物理撤销，解封不创建请求。

同时同步 7 个测试文件、消费者示例配置，以及 `AGENTS.md`、`Xboard源码与业务分析.md`、`功能清单.md`。源码分析文档中的历史验收与版本记录不代表本次测试环境已通过验收。

## 合并处理与保留的上游修复

- 两边共同修改的运行文件只有 `app/Jobs/OrderHandleJob.php`：保留目标版本的订单查询方式，仅追加跳过 Java 订单的保护，不恢复旧版事务外的 `lockForUpdate()`。
- 其余 29 个同步文件与来源版本逐文件 Git blob 校验一致。
- 保留目标仓库的重复退款和礼品卡兑换防护、余额抵扣后佣金计算、设备 IP 数组重排、Shadowrocket Hysteria2 参数、支付宝当面付时间戳修复，以及相应测试和翻译。
- 未修改远程地址、分支、提交历史、Docker 构建入口、依赖锁文件或实际环境配置；未复制 `.env`、`vendor`、运行日志、上传数据和本地密钥。

## 数据库与部署前提

新增的 Xboard 正式迁移：

- `database/migrations/2026_08_28_000000_harden_v2_order_for_java_user_orders.php`。
- `database/migrations/2026_08_28_010000_add_unique_uuid_to_v2_payment_table.php`。

执行前备份数据库，检查待执行迁移清单、同用户重复活动订单和重复支付 UUID。存在重复时只核对处理，不自动删除数据。订单迁移可能先增加部分字段后失败，失败后需检查实际结构，不能假定 DDL 全部回滚。

先完成 Xboard 原生结构升级，再启动依赖字段的 Java 版本。新版 Xboard 封禁功能上线前必须确认 Java Flyway V009 请求表已建立；Java 撤销消费者默认关闭，完整撤销能力需单独验收、启用。节点通知启用前必须确认 Java V007、Redis 与新版 WebSocket 进程就绪，Xboard 消费者保持唯一且默认关闭。

当前 Dockerfile 默认从上游仓库克隆代码，示例 Compose 使用上游镜像；实际构建/部署必须另行核对目标仓库、分支和镜像，不能只因本地已同步就认为镜像包含这些改动。Docker 启动入口会在识别为已安装时尝试运行 `xboard:update`，迁移失败后仍可能继续启动，必须检查迁移结果。单纯拉取代码或重启 PHP 不会自动迁移。

本次没有调整部署来源或运行开关。不要直接运行包含强制重置与依赖更新的 `update.sh`，不要使用数据库重建、清空或回滚命令。Java 订单开始写入后，不应未经评估退回没有订单隔离保护的原版 Xboard。

## 本次检查证据

- 26 个同步 PHP 文件：`php -l` 全部通过。
- 5 个目标单元测试文件：10 个测试、24 个断言通过，覆盖请求载荷、节点通知载荷/退避、连接发送结果、停用插件表单与 NULL 设置。
- 目标仓库尚无 `vendor`：测试临时复用来源仓库的已安装依赖，通过仓库外引导文件把业务类和测试类加载路径指向 `Xboard-test`；已用反射检查关键业务类实际路径，不属于目标仓库独立安装依赖后的验收。
- 没有运行会写共享数据库的封禁和流量并发集成测试，没有运行带 `RefreshDatabase` 的上游测试，没有执行迁移或连接真实节点。
- 未运行 PHPStan 和完整应用回归；目标仓库独立依赖环境、实际数据库与部署后运行仍需验证。

## 建议提交说明

标题：`feat: 同步 Java 用户后台配套改造与 Xboard 管理端兼容修复`

正文可概括为：订单处理权隔离和结构迁移、支付 UUID 唯一约束、停用支付插件编辑、空设置兼容、流量节点通知与重置并发保护、封禁撤销联动；保留测试仓库较新的上游修复，补齐测试与同步说明。
