# Xboard 源码与业务分析

> 文档导航：功能概览见 [Xboard 功能清单](功能清单.md)；Java 当前状态先看[用户后台当前状态摘要](../hop/docs/Java用户后台当前状态摘要.md)，需要决策时再看 [Java 用户后台实现计划与进度](../hop/docs/Java用户后台实现计划与进度.md)。本文是 Xboard 源码与调用链基线，不是 Java 首发验收报告。

> 本文档记录 Xboard 当前源码、数据库、路由和默认用户主题所体现的功能与业务基线。
>
> Java 的技术决策、模块任务、实现状态和测试证据记录在 [Java 用户后台实现计划与进度](../hop/docs/Java用户后台实现计划与进度.md)；模块历史明细按需查看 [模块详细实施归档](../hop/docs/Java用户后台模块详细实施归档.md)。
>
> 精简的功能总览见 [Xboard 功能清单](功能清单.md)。

## 0. 分析基线与刷新规则

- 最近一次完整整理日期：2026-08-31。
- 整理时 Xboard 分支：`master`。
- 源码基线：`f41c838`；其父提交为上游源码基线 `60ba571`。
- `f41c838` 包含本地 Java 用户订单处理权隔离、订单/支付结构 Migration 以及本文档，不包含 Java 订阅模块对 Xboard 订阅 Controller、Service、流量重置服务或 User Observer 的修改。
- 上述提交号用于判断本文档覆盖的源码范围，不替代接手时执行 `git status`、`git log -1` 和重新追踪目标模块完整调用链。
- Xboard 升级、重新拉取、切换分支或修改运行源码后，必须重新核对相关路由、Controller、Service、Model、Job、事件监听器、默认主题调用和全部 Migration；发现行为变化时同步更新本文档及 Java 实施进度文档。

本文中的事实分为三类：源码/迁移调用链是稳定基线；“当前部署页面实测基线”是日期化运行快照；Java 迁移和首发取舍属于项目决策，必须回到 Java 进度文档确认，不能仅凭本文件的“已接入”字样判断已验收。

### 0.1 高频模块源码快速导航

快速导航只用于定位入口，不替代对本次受影响调用链的完整阅读。首次分析新模块时，应把用户路由、管理操作、请求校验、资源转换、Service、Model、Job/定时任务、Migration 和当前主题调用补全后，再将稳定导航记入本节。

`order/payment` 当前导航：

- 业务说明：[订单与收银台](#34-订单与收银台)、[我的订单](#35-我的订单)、[订单管理](#47-订单管理)、[支付方式管理](#48-支付方式管理)、[优惠券管理](#49-优惠券管理)、[定时任务](#52-定时任务)。
- 路由：`app/Http/Routes/V1/UserRoute.php`、`app/Http/Routes/V1/GuestRoute.php`、`app/Http/Routes/V2/AdminRoute.php`。
- Controller/协议：`app/Http/Controllers/V1/User/OrderController.php`、`app/Http/Controllers/V1/Guest/PaymentController.php`、`app/Http/Controllers/V2/Admin/OrderController.php`、`app/Http/Requests/User/OrderSave.php`、`app/Http/Resources/OrderResource.php`。
- 核心业务：`app/Services/OrderService.php`、`app/Services/PaymentService.php`、`app/Services/CouponService.php`、`app/Services/UserService.php`、`app/Models/Order.php`。
- 异步/调度：`app/Jobs/OrderHandleJob.php`、`app/Console/Commands/CheckOrder.php`、`app/Console/Kernel.php`。
- 结构：`database/migrations/2023_03_19_000000_create_v2_tables.php`、`database/migrations/2026_08_28_000000_harden_v2_order_for_java_user_orders.php`、`database/migrations/2026_08_28_010000_add_unique_uuid_to_v2_payment_table.php`，并使用 `rg` 继续核对所有后续 order/payment Migration。

## 1. 项目定位与总体结构

Xboard 是一套订阅制代理节点销售与管理面板，核心业务不是普通会员系统，而是以下能力的组合：

1. 用户注册、登录、账户与多端会话管理。
2. 套餐展示、购买、续费、换购、一次性套餐、流量重置包。
3. 余额、优惠券、支付网关、异步支付回调与订单开通。
4. 用户订阅链接生成，以及按客户端类型动态输出不同格式的节点配置。
5. 节点、权限组、流量倍率、在线状态、用户流量和设备限制管理。
6. 邀请码、一级返佣和可选三级分销、佣金转余额或提现。
7. 公告、知识库、工单、邮件、Telegram Bot。
8. 礼品卡/兑换码奖励系统。
9. 管理统计、队列监控、审计日志、主题和插件管理。

技术结构：

- 后端：Laravel 12、PHP 8.2、Sanctum、Horizon、Octane。
- 管理前端：React + Shadcn UI（仓库当前未包含 `public/assets/admin` 的实际构建文件）。
- 用户前端：Vue 3 + TypeScript + Naive UI；仓库内为已编译的 `theme/Xboard/assets/umi.js`。
- 数据：MySQL/SQLite 兼容，Redis 用于缓存、队列、节点状态和在线状态。
- API：用户业务主要在 `/api/v1`；管理业务主要在 `/api/v2/{secure_path}`。

## 2. 角色与认证边界

### 2.1 角色

- 游客：读取公开配置和可售套餐，注册、登录、找回密码。
- 普通用户：管理自己的套餐、订单、流量、邀请、工单等。
- Staff：模型有 `is_staff`，中间件也存在，但当前主路由未形成独立完整后台。
- Admin：`is_admin=true`，可访问管理 API。
- 订阅客户端：使用用户订阅 Token 获取配置，不使用登录 Bearer Token。
- 节点进程：使用全局 `server_token` 或机器 Token 与面板通信。

### 2.2 两套用户凭证必须区分

1. 登录凭证：Sanctum Personal Access Token，接口返回格式为 `Bearer {token}`，有效期一年。
2. 订阅凭证：用户表 `token` 字段，用于 `/s/{token}` 或 `/api/v1/client/subscribe?token=...`。

另外，节点用户身份还会使用 `uuid`。执行“重置订阅安全信息”时，`token` 与 `uuid` 会同时更新，旧订阅地址和旧节点认证立即失效。

## 3. 用户后台功能清单

当前部署的默认 Xboard 主题菜单按页面顺序为：仪表盘、使用文档；“财务”分组下的我的订单、我的邀请；“订阅”分组下的购买订阅、节点状态；“用户”分组下的个人中心、我的工单、流量明细。登录、注册、忘记密码是公开独立页面；订单详情、套餐详情、工单详情是由业务数据和操作进入的隐藏页面，源码中存在，但本次空数据测试账户无法进入实测。

### 3.0 当前部署页面实测基线（历史快照）

> 本节记录对应日期的运行页面观察，不自动代表当前进程、数据库或主题状态。需要最新运行结论时，以 Java 验收证据归档和实时环境检查为准。

本节专门记录当前部署页面中真实可见的功能。后续各节仍会保留源码确认存在的后端能力，但会明确区分“当前页面已展示”“满足条件后展示”和“仅 API/扩展能力”。测试账户当前没有有效订阅、订单、邀请码、工单和流量记录，因此无法用该账户验证支付详情、订单详情、工单对话、节点卡片和有效订阅卡片等条件页面。

| 页面/区域 | 当前部署实际可见功能 |
|---|---|
| 登录页 `#/login` | 邮箱、密码、登入；进入注册、忘记密码；简体中文语言切换 |
| 注册页 `#/register` | 邮箱、密码、再次输入密码、可选邀请码；注册、返回登入、语言切换；当前配置没有邮箱验证码和验证码组件 |
| 忘记密码 `#/forgetpassword` | 邮箱、邮箱验证码、发送验证码、密码、再次输入密码、重置密码、返回登入、语言切换 |
| 全局导航/顶部栏 | 侧栏折叠、明暗主题、语言切换、全屏；用户菜单中的个人中心和登出 |
| 仪表盘 `#/dashboard` | “我的订阅”区域和购买订阅入口；查看教程、一键订阅、购买订阅、工单沟通四个快捷入口 |
| 使用文档 `#/knowledge` | 文档关键词输入框和搜索按钮；当前没有文章数据 |
| 我的订单 `#/order` | 订单号、周期、订单金额、订单状态、创建时间、操作列表；当前无订单 |
| 我的邀请 `#/invite` | 可用佣金、划转、提现；注册人数、佣金比例、确认中/累计佣金；邀请码生成与列表；佣金发放记录 |
| 购买订阅 `#/plan` | 全部/按周期/按流量筛选；套餐价格、流量、限速、设备数、服务说明和立即订阅 |
| 节点状态 `#/node` | 无有效订阅时显示无可用节点提示，并提供购买订阅链接 |
| 个人中心 `#/profile` | 消费余额；修改密码；到期/流量邮件提醒开关；重置订阅信息 |
| 我的工单 `#/ticket` | 新建工单；主题、级别、状态、创建时间、最后回复时间、操作列表；当前无工单 |
| 流量明细 `#/traffic` | 近一个月保留提示；记录时间、实际上行/下行、扣费倍率、总计列表；当前无记录 |

页面实测结论：当前主题没有礼品卡、活跃会话管理、邮件链接登录、快速登录、Telegram 绑定、官方客户端下载等可见入口。这些能力若在下文保留，均表示源码/API/插件能力，而不是当前部署的必做页面。

#### 3.0.1 源码存在但本次未触发实测的页面与功能

| 页面/功能 | 源码确认存在的能力 | 本次未触发原因 |
|---|---|---|
| 有效订阅仪表盘 | 套餐名称、到期与重置时间、流量统计、流量预警、复制/扫码订阅、客户端一键导入、协议选择 | 测试账户没有有效订阅 |
| 仪表盘公告与业务统计 | 公告轮播/弹窗、待支付订单数、处理中工单数、邀请用户数 | 当前站点或测试账户没有对应数据 |
| 套餐详情与收银台 | 周期选择、优惠券、余额抵扣、支付方式、手续费、支付状态轮询 | 为避免创建真实订单，本次只查看到套餐列表和“立即订阅”按钮 |
| 订单详情 `#/order/:trade_no` | 订单详情、继续付款、取消订单、支付结果 | 测试账户没有订单 |
| 节点卡片 | 节点名称、协议、倍率、标签和在线状态 | 测试账户没有有效订阅，因此页面只有无节点提示 |
| 知识文章详情 | 分类、文章详情、订阅权限内容和动态订阅地址占位符 | 当前站点没有可见知识文章 |
| 工单详情 `#/ticket/:id` | 消息记录、追加回复、自动刷新、关闭工单 | 测试账户没有工单，且本次没有创建测试工单 |
| 提现表单及提现工单 | 选择提现方式、填写账号、自动创建高优先级工单 | 账户佣金为 0，本次只确认到提现按钮 |
| 修改密码、提醒设置、重置订阅的提交结果 | 保存新密码、更新提醒偏好、刷新订阅 Token 与 UUID | 只检查页面控件，未修改账户数据 |
| 动态注册安全组件 | 注册邮箱验证码、reCAPTCHA/Turnstile、强制邀请码 | 当前部署配置未开启，因此注册页不显示 |

上述项目不是“已经在当前页面实测通过”，也不是凭空增加的需求；它们是源码确认存在、但需要特定数据、配置或写操作才能进入的业务能力。

### 3.1 游客与认证

#### 3.1.1 公开站点配置

- 获取服务条款地址、站点描述、站点 URL、Logo。
- 获取是否要求邮箱验证码。
- 获取是否强制邀请码注册。
- 获取邮箱后缀白名单。
- 获取验证码开关和类型：reCAPTCHA v2、reCAPTCHA v3、Cloudflare Turnstile。
- 获取各验证码站点公钥及 reCAPTCHA v3 分数阈值。
- 配置结果允许插件通过 Hook 扩展。

#### 3.1.2 注册

- 当前部署注册页只展示邮箱、密码、确认密码和可选邀请码；邮箱验证码、验证码组件、强制邀请码等字段是否出现由站点配置决定，当前配置未开启。
- 邮箱和密码注册，密码至少 8 位。
- 邮箱统一转为小写并去除首尾空白。
- 可开启 IP 注册频率限制：限定时间窗口内最多注册若干账号。
- 可开启验证码校验。
- 可开启邮箱域名白名单。
- 可禁止 Gmail `.` 和 `+` 别名注册。
- 可全局关闭注册。
- 可强制邀请码；邀请码可配置一次性使用或永久有效。
- 可要求 6 位邮箱验证码；验证码有效期 5 分钟，同邮箱 60 秒内不能重复发送。
- 注册后自动登录并生成登录 Token。
- 新用户可按系统配置自动分配试用套餐和试用时长。
- 记录最后登录时间，并触发注册前/后插件 Hook。

#### 3.1.3 登录

- 邮箱密码登录。
- 密码错误次数限制；达到阈值后临时锁定。
- 封禁账号禁止登录。
- 兼容旧密码算法和盐，修改或重置后升级为 PHP 默认密码哈希格式。
- 登录成功更新最后登录时间并创建一个新会话 Token。
- 返回登录凭证、订阅 Token、是否管理员。

#### 3.1.4 邮件链接登录和快速登录（仅后端能力，当前部署页面无入口）

- 当前登录页只有邮箱密码登录，不展示邮件链接登录或快速登录按钮。
- 可开启“邮件链接登录”。
- 请求成功时即使邮箱不存在也返回成功，避免泄露账号是否存在。
- 邮件登录链接 Token 默认 5 分钟有效，使用一次后删除。
- 已登录用户或持有有效 Bearer Token 的客户端可生成 60 秒有效的快速登录 URL。
- 支持传入登录后跳转页面。

#### 3.1.5 忘记密码

- 邮箱、6 位验证码、新密码重置。
- 同一邮箱失败尝试达到 3 次后短期限制。
- 成功后清除验证码。

### 3.2 仪表盘

- 当前测试账户没有有效订阅，页面实际只显示“我的订阅”的购买入口，以及查看教程、一键订阅、购买订阅、工单沟通四个快捷入口。
- 下列订阅数据、公告、统计、二维码和协议选择均为源码中存在的条件功能，只有站点有对应数据且用户满足条件时才会显示；本次空账户页面未触发。
- 展示当前套餐名称、长期有效/到期时间、距离到期天数。
- 展示上传、下载、总已用、总流量、使用百分比。
- 展示下次流量重置时间。
- 流量达到 70% 时提示购买流量重置包。
- 展示待支付订单数量、处理中工单数量、邀请用户数。
- 展示公告轮播；带“弹窗”标签的公告自动弹窗。
- 提供购买/续费、使用教程、工单、一键订阅快捷入口。
- 拉取可用节点并识别当前可用协议。
- 支持复制订阅、扫码订阅、按平台一键导入客户端。
- 支持按协议选择订阅内容，以及 HY2 单独订阅。

### 3.3 套餐与订阅商品

#### 3.3.1 套餐列表和详情

- 当前部署的套餐列表提供“全部、按周期、按流量”筛选；套餐卡片展示价格、流量、限速、设备数、服务说明和“立即订阅”。
- 游客和登录用户均可读取可售套餐。
- 仅返回 `show=true`、`sell=true` 且未达到容量上限的套餐。
- 当前用户续费原套餐时，依据 `renew` 判断，而不是只依据 `show`。
- 套餐字段包括：名称、标签、描述、权限组、流量 GB、限速 Mbps、设备数、容量限制、销售/展示/续费状态、排序、流量重置策略。
- 套餐描述支持变量：流量、限速、设备数、重置方式。

#### 3.3.2 计费周期

- 月付、季付、半年付、年付、两年付、三年付。
- 一次性套餐：无到期时间，按总流量有效。
- 流量重置包：只允许当前套餐有效的用户购买，支付完成后清零上/下行已用量。

#### 3.3.3 套餐购买限制

- 每个用户同一时间只能存在一个待支付或开通中的订单。
- 未配置对应周期价格时不可购买。
- 新购其他套餐时检查套餐容量。
- 隐藏但允许续费的套餐，只能由仍持有该套餐的有效用户续费。
- 可全局禁止换套餐。
- 不允许续费的套餐，当前用户不能继续购买同套餐。

### 3.4 订单与收银台

#### 3.4.1 订单类型

- 新购：用户无有效同套餐订阅。
- 续费：当前有效套餐与购买套餐相同。
- 换购/升级：当前有效套餐与购买套餐不同。
- 流量重置：购买当前套餐的流量重置包。

#### 3.4.2 创建订单时的金额处理顺序

1. 读取套餐周期原价。
2. 应用优惠券。
3. 应用用户专属折扣。
4. 识别订单类型。
5. 换套餐时计算旧套餐剩余价值并折抵。
6. 计算邀请佣金基数。
7. 自动使用账户余额抵扣。
8. 保存订单并返回订单号。

#### 3.4.3 换套餐折抵

- 可由系统开关控制是否计算旧套餐剩余价值。
- 一次性套餐按“剩余流量 × 原单位流量价格”计算。
- 周期套餐按剩余有效期比例计算；特定配置下取剩余时间比例与剩余流量比例中的较小值。
- 旧套餐价值超过新订单时，新订单为 0 元，多余价值转为用户余额。
- 被折抵的旧订单完成新订单开通后标记为“已折抵”。

#### 3.4.4 余额支付

- 下单时自动抵扣用户余额。
- 余额足够则订单剩余应付为 0，直接走免费订单开通。
- 取消待支付订单时返还已扣余额。
- 金额更新通过数据库事务和行锁保证一致性。

#### 3.4.5 优惠券

- 输入券码预检查。
- 固定金额券或百分比折扣券。
- 校验启用状态、开始/结束时间、全局剩余次数。
- 可限制套餐、购买周期和每用户使用次数。
- 优惠金额不会超过订单金额。

#### 3.4.6 支付

- 拉取已启用支付方式、图标、固定手续费和百分比手续费。
- 结账时计算手续费并记录支付方式。
- 支付插件返回类型和值：二维码/数据、跳转 URL 等。
- Stripe 类支付可单独读取公钥并提交 Token（代码保留兼容入口）。
- 异步回调按支付实例 UUID 定位配置、验签、防重复处理、写回第三方流水号。
- 0 元订单无需第三方支付，直接开通。
- 可轮询订单状态。

当前核心支付插件包括：支付宝当面付、BTCPay、CoinPayments、Coinbase Commerce、易支付 EPay、MGate。

#### 3.4.7 订单状态机

- `0 待支付 -> 1 开通中 -> 3 已完成`。
- `0 待支付 -> 2 已取消`。
- 已完成的旧订单在换购折抵后可变为 `4 已折抵`。
- 支付回调和后台手工确认都先转为“开通中”，再由订单任务开通套餐。
- 定时任务每分钟扫描待支付/开通中订单，保证失败后可重试。
- 待支付订单创建超过 2 小时会被订单任务自动取消并返还已抵扣余额。

#### 3.4.8 开通结果

- 周期套餐：写入套餐、权限组、总流量、限速、设备数并延长到期时间。
- 一次性套餐：到期时间设为 `null`，流量用尽才失效。
- 新购/换购可按系统事件配置清零流量。
- 流量重置包仅清零 `u/d`。
- 更新用户下次流量重置时间并记录重置日志。

### 3.5 我的订单

- 当前部署订单列表直接展示：订单号、周期、订单金额、订单状态、创建时间和操作。
- 后端接口支持查询全部订单或按 0/1/2/3 状态筛选；当前空列表页面没有展示独立筛选控件。
- 套餐、订单类型、支付方式、优惠、余额抵扣、手续费、旧套餐折抵和关联旧订单属于订单详情/结账数据，不是当前列表页固定列。
- 待支付订单继续付款或取消。
- 支付页每 1.5 秒轮询订单状态。
- 完成后跳转使用文档。

### 3.6 我的订阅和节点

#### 3.6.1 订阅信息

- 返回套餐、订阅地址、到期时间、上传/下载/总流量、限速、设备数、UUID、下次重置时间和剩余重置天数。
- 订阅链接可使用系统主站域名、独立订阅域名和自定义订阅路径。

#### 3.6.2 节点列表

- 当前 `UserService::isAvailable` 的实际判断是：账号未封禁、总流量额度非 0、套餐未到期；它没有检查 `u+d < transfer_enable`。因此流量已耗尽的用户仍可能拿到订阅/节点列表，但节点用户同步和超限任务会阻止其继续使用。Java 重写时应统一这两处“可用”定义。
- 节点按用户权限组匹配。
- 返回节点名称、协议、版本、倍率、标签、在线状态、缓存键和最后检查时间。
- 支持 ETag/304，减少重复加载。

#### 3.6.3 订阅输出

- 自动识别 User-Agent 或 `flag` 参数，选择对应配置生成器。
- 支持按 `types` 筛协议；支持 `filter` 按节点名称或标签过滤。
- 可在节点名称前增加协议前缀。
- 可把剩余流量、到期日、下次重置天数作为虚拟节点插入订阅。
- 响应带 `subscription-userinfo` 等客户端可识别头信息。

支持的订阅客户端/格式：

- General：v2rayN、v2rayNG、Passwall、SSRPlus、SagerNet 等通用链接。
- Clash、Clash Meta/Verge/FlClash/NekoBox/CFA。
- Sing-box、Hiddify、SFM。
- Surge、Surfboard、Stash。
- Shadowrocket、Quantumult X、Loon。
- 原生 Shadowsocks。

支持的节点协议：Hysteria/Hysteria2、VLESS、Trojan、VMess、TUIC、Shadowsocks、AnyTLS、SOCKS、Naive、HTTP、Mieru。

### 3.7 流量明细

- 当前部署页面明确提示“流量明细仅保留近一个月数据以供查询”，列表列为记录时间、实际上行、实际下行、扣费倍率和总计。
- 用户接口查询当前月流量记录。
- 每条记录包含实际上行、实际下行、节点倍率、倍率后扣除流量和记录时间。
- 后台用户/节点统计表的定时清理周期约为两个月；这是后台统计保留策略，不代表用户页面可查询两个月。
- 用户达到流量上限后，定时任务通知在线节点移除该用户。
- 支持按套餐策略自动重置：每月 1 日、按到期日每月、永不重置、每年 1 月 1 日、按到期日每年、跟随系统。

### 3.8 公告与知识库

#### 公告

- 用户只查看启用公告，按管理员排序和 ID 倒序显示。
- 默认每页 5 条。
- 支持标题、富文本内容、图片、标签和弹窗效果。

#### 知识库

- 当前部署页展示关键词输入框和“搜索”按钮；测试环境当前没有可见文章，因此分类、详情和订阅权限内容未在页面实测触发。
- 按语言、分类返回文章；支持标题/正文关键词搜索。
- 支持文章详情。
- 无有效订阅时，`<!--access start-->...<!--access end-->` 区域替换为无权限提示。
- 正文支持动态占位符：站点名、订阅 URL、URL 编码订阅 URL、安全 Base64 订阅 URL。
- 支持插件修改最终文章资源。

### 3.9 工单

- 当前部署列表页提供“新的工单”按钮，列表列为主题、工单级别、工单状态、创建时间、最后回复时间和操作。
- 工单详情页由已有工单进入；测试账户无工单，本次未实际进入详情页。
- 创建工单：主题、优先级（低/中/高）、首条消息。
- 用户追加回复，详情区默认每 2 秒刷新。
- 用户主动关闭工单。
- 可配置“用户必须等待客服回复后才能再次回复”。
- 工单状态：开启/关闭；回复状态：待回复/已回复。
- 已由客服回复且 24 小时无更新的工单自动关闭。
- 创建和回复可通知 Telegram 管理员。

佣金提现复用工单系统：用户选择提现方式并填写账号后，系统自动创建高优先级提现工单。需要满足最低提现金额，且提现功能未关闭。

### 3.10 邀请与返佣

- 当前部署页面展示可用佣金、划转、推广佣金提现、已注册用户数、佣金比例、确认中佣金、累计佣金、邀请码管理和佣金发放记录。
- 生成 8 位邀请码；限制每用户可拥有的未使用邀请码数量。
- 邀请码可一次性或永久有效。
- 注册落地页会在后端上报邀请码 PV，但当前页面的邀请码表格只展示邀请码和创建时间，不展示访问量。
- 显示邀请注册人数、累计有效佣金、确认中佣金、佣金比例、可用佣金。
- 当前页面的“佣金发放记录”表格展示发放时间和佣金；关联订单号、订单金额等属于后端返佣明细数据，不是当前页面固定列。
- 用户可有专属佣金比例和佣金类型：跟随系统、每次订单、仅首次有效订单。
- 订单完成 3 天后可自动确认佣金。
- 支持一级返佣或可选三级分销比例。
- 佣金可进入独立佣金余额，也可配置为直接进入消费余额。
- 用户可将佣金余额划转到消费余额；该操作不可逆。
- 可通过工单申请提现。

### 3.11 礼品卡/兑换码（后端已实现，默认主题无菜单）

- 检查兑换码并预览奖励、套餐信息、可兑换状态和不可兑换原因。
- 执行兑换并返回本人与邀请人的实际奖励。
- 查询本人兑换历史、分页和详情。
- 查询可用礼品卡类型。
- 实际实现的类型：通用礼品卡、套餐礼品卡、盲盒礼品卡。
- 奖励类型：余额、流量、设备数、流量重置、套餐、有效期延长、邀请人奖励。
- 使用条件：新用户、付费用户、指定套餐、必须有邀请人。
- 使用限制：每用户最大次数、冷却小时数、兑换码最大总使用次数和过期时间。
- 盲盒按权重抽取奖励；支持指定时间范围内节日倍率。

### 3.12 个人中心与安全

- 顶部栏展示用户头像和邮箱，用户菜单提供“个人中心”和“登出”。
- 当前个人中心页面展示消费余额；不展示创建时间、最后登录、佣金、套餐详情或 Telegram 绑定状态。
- 开关到期邮件提醒和流量邮件提醒。
- 修改密码，要求旧密码正确、新密码至少 8 位。
- 修改密码后保留当前会话、删除其他会话。
- 查询全部活跃会话和移除指定会话仅为 API 能力，当前主题没有会话管理入口。
- 重置订阅 Token 与 UUID。
- 获取快速登录链接仅为 API 能力，当前主题没有入口。

### 3.13 Telegram（Bot/插件能力，当前部署页面无入口）

- 当前个人中心和全局菜单均未展示 Telegram 绑定入口。
- 用户端读取 Bot 用户名并展示绑定教程。
- Bot 支持 `/start`、`/bind`、`/traffic`、`/getlatesturl`、`/unbind`。
- Bot 可按订阅地址绑定账号、查询流量、获取最新订阅地址、解绑。
- 有有效订阅的已绑定用户可自动获准加入 Telegram 群。
- 新工单、用户回复和支付成功可通知管理员。
- 管理员可直接回复 Telegram 工单提醒消息，回写为后台工单回复。

### 3.14 官方客户端兼容接口（当前部署页面无入口）

- 输出客户端 Clash 配置。
- 根据客户端 User-Agent 返回 Windows/macOS/Android 版本与下载地址。

## 4. 管理后台功能清单

### 4.1 管理仪表盘与统计

- 今日、昨日、本月、上月收入及环比。
- 本月/上月已发佣金、待确认佣金和增长率。
- 本月新用户、总用户、有效用户和增长率。
- 在线用户、在线设备、在线节点。
- 待处理工单数。
- 今日、本月、历史总上行/下行/总流量。
- 节点今日/昨日流量排行。
- 用户或节点指定时间段 Top 10 流量排行，并与上一周期比较。
- 订单收入/佣金趋势、笔数、均值和佣金占比。
- 注册、邀请、流量等历史统计记录。
- 指定用户的流量统计明细。

### 4.2 用户管理

- 用户分页、组合筛选、AND/OR 逻辑、关系字段筛选和多字段排序。
- 可筛字段包括 ID、邮箱、套餐、权限组、邀请人、封禁、余额、流量、到期、角色等。
- 查看用户详情、套餐、权限组、邀请关系和订阅地址。
- 编辑邮箱、密码、总流量、已用上下行、到期、套餐、余额、佣金余额、佣金比例、佣金类型、用户折扣、限速、设备数、备注、管理员/员工角色。
- 设置或清除邀请人。
- 封禁/解封。当前工作区集成中，单用户保存和批量封禁在写入 `v2_user.banned=1` 的同一数据库事务内向 Java Flyway V009 表 `java_identity_revocation_request` 插入 `ACCOUNT_BANNED` 请求；Java identity 是唯一消费者，物理撤销全部 Java Refresh Token 和登录会话。Xboard 不再直接删除 Java 会话；请求写入失败时封禁事务回滚。
- 重置订阅 Token 和 UUID。
- 单个、随机批量、指定前缀批量生成用户，最多 500 个。
- 生成用户时可指定邮箱后缀、密码、套餐、到期时间。
- 批量生成后可导出账号 CSV。
- 按选中用户、当前筛选结果或全部用户导出 CSV。
- 按选中、筛选或全部用户批量发邮件。
- 删除用户，并同步通知节点移除用户。

identity 封禁撤销集成的运行边界：

- Xboard 只创建来源为单用户或批量管理操作的非敏感请求，不创建 Java 表、不 claim 请求、不写处理状态，也不修改 `java_refresh_token`。
- Java 消费器默认关闭；应用 V009、部署 Xboard 生产者和 Java 消费代码，并用隔离账号确认请求写入、claim 和旧凭证失效后才显式启用。
- Java 使用数据库 claim token、租约和有界重试支持多实例恢复；终态请求保留用于审计。回滚先关闭 Java 消费器并保留请求，不恢复 Xboard 直接删会话的旧链路。
- 2026-09-02 已用共享库 `test_` 数据通过真实 Laravel 管理 HTTP Kernel 的单用户和 `selected` 批量封禁、消费者关闭状态及生产者故障回滚；为保护共享库未对 `all` 范围做全量实写。

### 4.3 套餐管理

- 套餐列表、排序、用户总数和有效用户数。
- 创建/编辑套餐：名称、描述、标签、权限组、流量、各周期价格、限速、设备数、容量、重置策略。
- 可强制把套餐的权限组、流量、限速、设备数同步给所有现有用户。
- 独立切换展示、销售和续费状态。
- 删除前检查关联订单和用户。

### 4.4 节点和服务器管理

- 节点列表、父节点、权限组、在线状态、流量和负载信息。
- 创建/编辑 11 类节点协议。
- 公共字段：名称、协议、地址、连接端口、服务端口、权限组、父节点、路由、标签、倍率、时段倍率、显示、启用、机器、流量上限、IP/排除项、自定义出站、自定义路由、证书配置。
- 协议配置：TLS/Reality/ECH/uTLS、多路复用、Brutal、WebSocket/gRPC 等网络参数、混淆、带宽和拥塞控制。
- 生成 ECH X25519 密钥和客户端配置。
- 复制节点；副本默认隐藏并清空流量及唯一 Code。
- 节点排序、显示/隐藏、启用/停用、绑定/解绑机器。
- 批量删除、批量更新、单个/批量清零节点流量。

### 4.5 机器管理

- 创建、编辑、启用/停用、删除节点机器。
- 为机器生成或重置独立 Token。
- 生成 xboard-node 机器模式一键安装命令。
- 查看机器下节点。
- 查看最后心跳、当前 CPU/内存/磁盘/网络负载。
- 查看 1～24 小时或指定点数的负载历史。
- 删除机器时自动解除关联节点并通知 WebSocket 注册中心。

### 4.6 权限组与路由规则

- 权限组增删改查，显示关联用户和节点数量。
- 被节点、套餐或用户引用的权限组不可删除。
- 路由规则增删改查。
- 路由规则包含备注、匹配表达式、动作和动作值，用于节点出站/分流。

### 4.7 订单管理

- 订单分页、筛选、排序和返佣订单视图。
- 查看用户、套餐、邀请人、佣金记录、折抵订单和支付流水。
- 后台手动确认待支付订单为已支付。
- 后台取消待支付订单并返还余额。
- 修改佣金状态。
- 管理员为指定邮箱手工分配套餐并创建订单，可指定周期和金额。

### 4.8 支付方式管理

- 从已启用插件中读取可用支付网关。
- 动态读取每个网关的配置表单。
- 创建/编辑显示名称、图标、网关、配置、通知域名、固定/百分比手续费。
- 启用/停用、排序和删除。
- 显示每个实例的完整异步通知 URL。

### 4.9 优惠券管理

- 优惠券分页、筛选和排序。
- 创建/编辑固定金额券和百分比券。
- 设置起止时间、总使用次数、每用户次数、限定套餐和周期。
- 单个生成指定或随机券码。
- 最多批量生成 500 个并输出 CSV。
- 启用/停用和删除。

### 4.10 礼品卡管理

- 礼品卡模板分页、按类型/状态筛选。
- 创建/编辑模板的描述、类型、条件、奖励、限制、特殊配置、图标、背景、主题色和排序。
- 启用/停用模板；有关联兑换码的模板不可删除。
- 单批最多生成 10000 个兑换码，设置前缀、有效小时、最大使用次数，并支持 CSV/文本导出。
- 兑换码按模板、批次和状态筛选。
- 启用/禁用、修改过期时间/次数/状态、删除未使用兑换码。
- 查询使用记录、本人奖励、邀请人奖励和倍率。
- 查看模板数、有效模板数、兑换码数、已用数、使用总数、每日趋势和类型分布。

### 4.11 工单管理

- 工单分页、筛选、排序和详情。
- 查看用户资料与完整消息链。
- 管理员回复并更新回复状态。
- 关闭工单。

### 4.12 公告与知识库管理

- 公告创建/编辑/删除、启用/停用、排序。
- 公告支持图片、标签和弹窗标记。
- 知识文章创建/编辑/删除、启用/停用、排序。
- 知识库支持语言、分类、标题和富文本正文。

### 4.13 站点与业务配置

- 邀请返佣：强制邀请码、默认佣金率、邀请码数量/永久有效、首次返佣、自动确认、提现门槛/方式、三级分销。
- 站点：Logo、HTTPS、关闭注册、名称、描述、主 URL、订阅 URL、试用套餐/小时、服务条款、币种/符号、工单等待回复。
- 订阅：换套餐、剩余价值折抵、默认重置方式、新购/续费/换购事件、订阅信息节点、协议前缀、默认邮件提醒、自定义订阅路径。
- 前端：当前用户主题、侧栏/头部/主题色、背景。
- 节点：全局通信 Token、拉取/上报间隔、设备限制模式、WebSocket 开关与地址。
- 邮件：SMTP 主机、端口、用户名、密码、加密、发件人、提醒开关和测试邮件。
- Telegram：Bot 开关、Token、自定义 Webhook 地址、讨论组链接、Webhook 注册和命令注册。
- 客户端：Windows/macOS/Android 版本和下载地址。
- 安全：安全域名模式、后台安全路径、邮箱验证/白名单、Gmail 别名限制、验证码、IP 注册限制、密码错误限制。
- 订阅模板：Sing-box、Clash、Clash Meta、Stash、Surge、Surfboard。

### 4.14 邮件模板

- 管理邮箱验证码、站点通知、到期提醒、流量提醒、邮件登录五种模板。
- 查看必需和可选占位符。
- 编辑主题和 HTML 内容，保存前检查必需占位符。
- 恢复内置默认模板。
- 向指定邮箱发送测试邮件。

### 4.15 主题和插件

- 列出、上传 ZIP、删除用户主题。
- 切换当前主题并编辑主题自定义配置。
- 默认主题配置包括主题色、背景图、自定义页脚 HTML。
- 插件按类型筛选，上传、安装、升级、启用、停用、卸载和删除。
- 动态读取和修改插件配置。
- 插件可注册 Hook、路由、视图、命令、定时任务、资源和数据库结构版本脚本。

### 4.16 系统运维

- 检查 Laravel Scheduler 是否正常心跳。
- 检查 Horizon 队列状态、进程、吞吐、等待时间和失败任务。
- 查看队列负载和 Master Supervisor。
- 查看失败任务列表。
- 查看管理审计日志，可按管理员、动作和关键词筛选。
- 审计记录管理 API 的 URI、方法、请求数据、IP、响应码等。

### 4.17 流量重置管理

- 按用户、邮箱、重置类型、触发来源、日期筛选重置日志。
- 查看重置前后上/下行及总流量。
- 查看最近 1～365 天自动/手工/定时重置统计。
- 管理员为有效套餐用户手工重置并填写原因。
- 查看单用户重置次数、上次/下次重置和历史。

## 5. 节点侧与后台自动任务

### 5.1 节点通信

- 节点握手、拉取配置、拉取可用用户。
- 上报用户流量、在线用户、存活设备、节点状态和负载指标。
- 机器模式一次管理多个节点。
- 通过 WebSocket 增量同步用户新增、更新和移除。
- 节点流量上报按倍率计入用户流量和统计表。

### 5.2 定时任务

- 每分钟检查订单并重试开通。
- 每分钟确认和发放佣金。
- 每分钟关闭满足条件的超时工单。
- 每分钟处理流量超限用户并通知节点。
- 每分钟执行到期流量重置。
- 每天生成业务统计。
- 每天发送到期/流量提醒邮件。
- 每 5 分钟生成 Horizon 快照、清理过期在线状态。
- 每天清理日志；流量统计保留约 2 个月，管理审计保留约 3 个月。

### 5.3 Hop Java traffic Outbox 短期消费方案

- 处理权：Xboard 继续是唯一节点控制面。Hop Java 只在业务事务写 `java_traffic_event_outbox`，不新增 WebSocket、machine 或 V1 节点消费者。
- 调度入口：现有每分钟 `check:traffic-exceeded` 在 `XBOARD_JAVA_TRAFFIC_OUTBOX_CONSUMER_ENABLED=true` 时额外调用 `JavaTrafficOutboxService`；默认关闭。`--dry-run` 只读检查 Outbox 与交付账本，不抢占、不发布，也不弹出旧的 `traffic:pending_check` Redis 集合。
- 持久交付：Java Flyway V007 创建 `java_traffic_node_delivery`。Xboard 用 `(outbox_event_id,node_id)` 唯一键、`PENDING/PROCESSING/DELIVERED`、claim token、租约和有界指数退避实现跨调度实例抢占与恢复；全部目标终态后才条件标记原 Outbox 为 `PROCESSED`。
- 协议复用：每次交付前通过 `ServerService` 与 `server.users.get` Hook 重新计算当前用户是否可用，再通过现有 `sync.user.delta` 发送带稳定 `event_id` 的 ADD/REMOVE。`NodeRegistry` 继续为 machine 连接补目标 `node_id`。
- 确认边界：Redis 发布成功只表示 Workerman 收到消息；Workerman 活动连接直接写出或接受进入发送缓冲后才以 delivery ID 与 claim token 确认，这只是短期传输层确认而非节点应用层 ACK。Redis 无订阅者、连接缓存失配、连接/缓冲拒绝或消费者崩溃会重试/租约回收；离线 WS 节点和 V1 轮询节点依靠重连或下次拉取的全量用户列表收敛。
- 启用与回滚：先应用 Hop Flyway V007，再部署 Xboard 消费代码和 Workerman，确认 Redis/WS 正常后才打开 Xboard 开关。回滚先关闭该开关并保留 Outbox/交付账本对账，不能让 Java 与 Xboard 同时消费，也不能删除未完成记录。
- 当前证据仅包含语法、静态分析、纯单元测试、本地 V007 和 `--dry-run`；尚未对真实节点、machine、多 Xboard 实例或故障恢复进行外部验收，生产开关保持关闭。

## 6. Xboard 数据和兼容性基线

1. 金额：用户余额、订单、佣金、优惠券固定金额均使用“分”的整数；套餐 `prices` 当前以“元”的数值存 JSON，下单时乘 100。
2. 流量：用户 `u/d/transfer_enable` 和统计为字节；套餐 `transfer_enable` 是 GB，下单开通时乘 `1024^3`。
3. 时间：大量旧表是 Unix 秒；新节点、机器表和流量重置日志使用 SQL `datetime`。
4. 永久有效：`expired_at=null`；不能把 `0` 当成永久，现有活跃判断中 `0` 表示已过期。
5. 套餐周期：API 仍使用 `month_price` 等旧名，数据库订单已逐步改用 `monthly` 等新名。
6. 返回结构：现有 API 同时存在 `{data}`、`{data,total}`、`{data,pagination}` 和裸响应。
7. 登录 Token 与订阅 Token 是两类不同凭证；订阅 Token 不具备用户后台访问权限。
8. 当前部分变更操作使用 GET，例如生成邀请码、重置订阅安全信息。
9. 管理 API 路径包含动态 `secure_path`，但实际权限仍由管理员认证和权限规则决定。
10. 当前部署主题没有礼品卡、活跃会话管理、邮件链接登录、快速登录、Telegram 绑定和官方客户端下载入口，但相关后端 API 或插件能力存在。
11. Telegram 用户解绑的控制器方法未形成可用用户路由，实际解绑由 Bot 插件完成。
12. 礼品卡建表版本脚本的注释列出 10 种类型，但当前 Model 和 Service 实际只实现 3 种，应以实际执行代码为准。
13. 管理端静态构建文件不在当前仓库，因此本文管理清单以服务端真实能力为准，不保证每个 API 在当前管理 UI 都有可见菜单。
14. `GET /user/knowledge/getCategory`、管理端公告 `update`、管理端用户 `setInviteUser` 等路由在当前代码中指向不存在的方法，属于遗留或失效入口。
15. `GiftCardCheckRequest` 当前没有校验规则，而兑换接口要求 8～32 位字符串；查询和兑换的格式校验并不一致。

## 7. 用户端 API 对照表

### 7.1 Passport/Guest

- `POST /api/v1/passport/auth/register`：注册。
- `POST /api/v1/passport/auth/login`：密码登录。
- `GET /api/v1/passport/auth/token2Login`：临时 Token 登录/跳转。
- `POST /api/v1/passport/auth/forget`：重置密码。
- `POST /api/v1/passport/auth/getQuickLoginUrl`：通过 Bearer Token 获取快速登录 URL。
- `POST /api/v1/passport/auth/loginWithMailLink`：发送邮件登录链接。
- `POST /api/v1/passport/comm/sendEmailVerify`：发送邮箱验证码。
- `POST /api/v1/passport/comm/pv`：记录邀请码访问量。
- `GET /api/v1/guest/comm/config`：公开配置。
- `GET /api/v1/guest/plan/fetch`：公开套餐。
- `GET|POST /api/v1/guest/payment/notify/{method}/{uuid}`：支付回调。
- `POST /api/v1/guest/telegram/webhook`：Telegram Webhook。

### 7.2 User

- 账户：`resetSecurity`、`info`、`changePassword`、`update`、`getSubscribe`、`getStat`、`checkLogin`、`transfer`、`getQuickLoginUrl`、`getActiveSession`、`removeActiveSession`。
- 订单：`order/save`、`order/checkout`、`order/check`、`order/detail`、`order/fetch`、`order/getPaymentMethod`、`order/cancel`。
- 套餐：`plan/fetch`。
- 邀请：`invite/save`、`invite/fetch`、`invite/details`。
- 内容：`notice/fetch`、`knowledge/fetch`、`knowledge/getCategory`。
- 工单：`ticket/save`、`ticket/fetch`、`ticket/reply`、`ticket/close`、`ticket/withdraw`。
- 节点与流量：`server/fetch`、`stat/getTrafficLog`。
- 营销：`coupon/check`、`gift-card/check`、`gift-card/redeem`、`gift-card/history`、`gift-card/detail`、`gift-card/types`。
- 通用：`comm/config`、`comm/getStripePublicKey`、`telegram/getBotInfo`。

### 7.3 Client

- `GET /{subscribe_path}/{token}`：主订阅地址。
- `GET /api/v1/client/subscribe?token=...`：兼容订阅地址。
- `GET /api/v1/client/app/getConfig`：客户端配置。
- `GET /api/v1/client/app/getVersion`：客户端下载版本。

## 8. 主要数据表

- 用户与认证：`v2_user`、`personal_access_tokens`。
- 商品交易：`v2_plan`、`v2_order`、`v2_payment`、`v2_coupon`。
- 邀请返佣：`v2_invite_code`、`v2_commission_log`。
- 内容支持：`v2_notice`、`v2_knowledge`、`v2_ticket`、`v2_ticket_message`。
- 节点：`v2_server`、`v2_server_group`、`v2_server_route`、`v2_server_machine`、`v2_server_machine_load_history`。
- 流量统计：`v2_stat_user`、`v2_stat_server`、`v2_stat`、`v2_traffic_reset_logs`。
- 礼品卡：`v2_gift_card_template`、`v2_gift_card_code`、`v2_gift_card_usage`。
- 系统：`v2_settings`、`v2_subscribe_templates`、`v2_mail_templates`、`v2_mail_log`、`plugins`、`v2_admin_audit_log`。

这份清单可作为 Java 重写的功能范围基线；真正进入编码前，建议继续产出三份可执行设计：统一 API 契约、数据库 ER 与结构版本管理方案、订单与流量的状态机/时序图。
